---
layout: post
title: "两数之和"
date: 2026-03-28 21:44 +0800
categories: [算法]
tags: [数组, 哈希表]
---

## 📖 目录
- [原题链接](#-原题链接)
- [题目描述](#-题目描述)
- [解题思路](#-解题思路)
- [题解](#-题解)

## 🔗 原题链接
- [https://leetcode.cn/problems/two-sum/description/](https://leetcode.cn/problems/two-sum/description/)

## 📝 题目描述
- 给定一个整数数组`nums`和一个整数目标值`target`，请你在该数组中找出 **和为目标值`target`** 的那 **两个** 整数，并返回它们的数组下标。

- 你可以假设每种输入只会对应一个答案，并且你不能使用两次相同的元素。

- 你可以按任意顺序返回答案。

## 🤔 解题思路
- 遍历数组`nums`，将每个元素的索引作为值存入哈希表，键为元素的值。
- 对于每个元素`nums[i]`，判断`target - nums[i]`是否在哈希表中，若存在，则返回对应的值（原数组索引），否则继续遍历。

## 🧑‍💻 题解
```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        table = dict()
        for i, num in enumerate(nums):
            if target - num in table:
                return [table[target - num], i]
            table[num] = i
```