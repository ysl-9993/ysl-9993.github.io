---
layout: post
title: "字符串相加"
date: 2026-03-25 22:44 +0800
categories: [算法]
tags: [字符串, 数学, 模拟]
---

## 📖 目录
- [原题链接](#-原题链接)
- [题目描述](#-题目描述)
- [解题思路](#-解题思路)
- [题解](#-题解)

## 🔗 原题链接
- [https://leetcode.cn/problems/add-strings/description/](https://leetcode.cn/problems/add-strings/description/)

## 📝 题目描述
- 给定两个字符串形式的非负整数`num1`和`num2`，计算它们的和并同样以字符串形式返回。

- 你不能使用任何内建的用于处理大整数的库（比如`BigInteger`），也不能直接将输入的字符串转换为整数形式。

## 🤔 解题思路
- 模拟竖式加法计算，从最低位相加，处理进位。
- 注意判断循环结束后是否留有进位需要处理。

## 🧑‍💻 题解
```python
class Solution:
    def addStrings(self, num1: str, num2: str) -> str:
        i = len(num1) - 1
        j = len(num2) - 1
        carry = 0
        res = ""
        while i >= 0 or j >= 0:
            n = int(num1[i]) if i >= 0 else 0
            m = int(num2[j]) if j >= 0 else 0
            res = str((n + m + carry) % 10) + res
            carry = (n + m + carry) // 10
            i -= 1
            j -= 1
        if carry:
            res = str(carry) + res
        return res
```