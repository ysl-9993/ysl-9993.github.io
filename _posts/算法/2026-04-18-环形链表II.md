---
layout: post
title: "环形链表II"
date: 2026-04-18 10:53 +0800
categories: [算法]
tags: [链表, 哈希表]
---

## 📖 目录
- [原题链接](#-原题链接)
- [题目描述](#-题目描述)
- [解题思路](#-解题思路)
- [题解](#-题解)

## 🔗 原题链接
- [https://leetcode.cn/problems/linked-list-cycle-ii/description/](https://leetcode.cn/problems/linked-list-cycle-ii/description/)

## 📝 题目描述
- 给定一个链表的头节点`head`，返回链表开始入环的第一个节点。如果链表无环，则返回`null`。

- 如果链表中有某个节点，可以通过连续跟踪`next`指针再次到达，则链表中存在环。为了表示给定链表中的环，评测系统内部使用整数`pos`来表示链表尾连接到链表中的位置（索引从`0`开始）。如果`pos`是`-1`，则在该链表中没有环。注意：`pos`不作为参数进行传递，仅仅是为了标识链表的实际情况。

- **不允许修改** 链表。

## 🤔 解题思路
- 遍历链表，使用哈希表存储已经访问过的节点，如果再次访问到已经访问过的节点，则说明存在环，返回该节点。
- 如果遍历结束没有访问到已经访问过的节点，则说明链表无环，返回`null`。

## 🧑‍💻 题解
- 时间复杂度：`O(n)`
- 空间复杂度：`O(n)`
    ```python
    # Definition for singly-linked list.
    # class ListNode:
    #     def __init__(self, x):
    #         self.val = x
    #         self.next = None

    class Solution:
        def detectCycle(self, head: Optional[ListNode]) -> Optional[ListNode]:
            table = set()
            p = head
            while p:
                if p in table:
                    return p
                else:
                    table.add(p)
                    p = p.next
            return None
    ```

<script>
MathJax = {
  tex: {
    inlineMath: [['$', '$'], ['\\(', '\\)']],
    displayMath: [['$$', '$$'], ['\\[', '\\]']],
    processEscapes: true
  }
};
</script>
<script src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js" id="MathJax-script" async></script>