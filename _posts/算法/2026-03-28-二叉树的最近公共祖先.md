---
layout: post
title: "二叉树的最近公共祖先"
date: 2026-03-28 19:12 +0800
categories: [算法]
tags: [树, 二叉树, 递归]
---

## 📖 目录
- [原题链接](#-原题链接)
- [题目描述](#-题目描述)
- [解题思路](#-解题思路)
- [题解](#-题解)

## 🔗 原题链接
- [https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree/description/](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree/description/)

## 📝 题目描述
- 给定一个二叉树, 找到该树中两个指定节点的最近公共祖先。

- 百度百科中最近公共祖先的定义为：“对于有根树`T`的两个节点`p`、`q`，最近公共祖先表示为一个节点`x`，满足`x`是 `p、q`的祖先且`x`的深度尽可能大（一个节点也可以是它自己的祖先）。”

## 🤔 解题思路
- 采用递归的方式，从根节点`root`开始，递归地遍历左右子树。
- 递归终止条件：
    - 如果当前节点为空，直接返回`None`。
    - 如果当前节点是`p`或`q`，则返回当前节点`root`。
- 递归过程：
    - 递归左子树，记录返回值为`left`。
    - 递归右子树，记录返回值为`right`。
- 返回值：
    - 如果`left`和`right`都不为`None`，则说明当前节点就是最近公共祖先，直接返回当前节点`root`。
    - 如果`left`为`None`，说明`p`或`q`在右子树中，直接返回`right`。
    - 如果`right`为`None`，说明`p`或`q`在左子树中，直接返回`left`。


## 🧑‍💻 题解
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, x):
#         self.val = x
#         self.left = None
#         self.right = None

class Solution:
    def lowestCommonAncestor(self, root: 'TreeNode', p: 'TreeNode', q: 'TreeNode') -> 'TreeNode':
        if root == None:
            return None
        if p == root or q == root:
            return root
        left = self.lowestCommonAncestor(root.left, p, q)
        right = self.lowestCommonAncestor(root.right, p, q)
        if left and right:
            return root
        elif left:
            return left
        else:
            return right
```